# Effect to populate the list of valid scenes for the UI
populate_valid_scenes_list = {
	# Clear existing list and slot variables
	remove_variable_list = valid_scenes_list
	remove_variable = votc_scene_1
	remove_variable = votc_scene_2
	remove_variable = votc_scene_3
	remove_variable = votc_scene_4
	remove_variable = votc_scene_5
	remove_variable = votc_scene_6
	remove_variable = votc_scene_7
	remove_variable = votc_scene_8
	remove_variable = votc_scene_9
	remove_variable = votc_scene_10
	
	# --- Generic Scenes (Prioritized) ---
	
	if = {
		limit = { is_valid_scene_throneroom = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_throneroom }
	}
	if = {
		limit = { is_valid_scene_garden = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_garden }
	}
	if = {
		limit = { is_valid_scene_bedchamber = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_bedchamber }
	}
	if = {
		limit = { is_valid_scene_tavern = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_tavern }
	}
	if = {
		limit = { is_valid_scene_market = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_market }
	}
	if = {
		limit = { is_valid_scene_dungeon = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_dungeon }
	}
	if = {
		limit = { is_valid_scene_feast = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_feast }
	}
	if = {
		limit = { is_valid_scene_temple = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_temple }
	}
	if = {
		limit = { is_valid_scene_alley = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_alley }
	}
	if = {
		limit = { is_valid_scene_armycamp = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_armycamp }
	}
	if = {
		limit = { is_valid_scene_courtyard_night = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_courtyard_night }
	}
	if = {
		limit = { is_valid_scene_camp = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_camp }
	}
	if = {
		limit = { is_valid_scene_study = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_study }
	}
	if = {
		limit = { is_valid_scene_councilchamber = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_councilchamber }
	}
	if = {
		limit = { is_valid_scene_battlefield = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_battlefield }
	}
	
	# --- Specific / Landmark Scenes ---
	
	if = {
		limit = { is_valid_scene_iron_throne = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_iron_throne }
	}
	if = {
		limit = { is_valid_scene_harem = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_harem }
	}
	if = {
		limit = { is_valid_scene_lingyinsi = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_lingyinsi }
	}
	if = {
		limit = { is_valid_scene_throneroom_japan = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_throneroom_japan }
	}
	if = {
		limit = { is_valid_scene_shaolinsi = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_shaolinsi }
	}
	if = {
		limit = { is_valid_scene_wudangshandaoguan = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_wudangshandaoguan }
	}
	if = {
		limit = { is_valid_scene_leshandafou = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_leshandafou }
	}
	if = {
		limit = { is_valid_scene_taishan = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_taishan }
	}
	if = {
		limit = { is_valid_scene_wulingyuan = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_wulingyuan }
	}
	if = {
		limit = { is_valid_scene_kaifenghuangcheng = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_kaifenghuangcheng }
	}
	if = {
		limit = { is_valid_scene_tengwangge = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_tengwangge }
	}
	if = {
		limit = { is_valid_scene_yueyanglou = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_yueyanglou }
	}
	if = {
		limit = { is_valid_scene_huanghelou = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_huanghelou }
	}
	if = {
		limit = { is_valid_scene_yungangshiku = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_yungangshiku }
	}
	if = {
		limit = { is_valid_scene_market_east = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_market_east }
	}
	if = {
		limit = { is_valid_scene_tavern_east = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_tavern_east }
	}
	if = {
		limit = { is_valid_scene_throneroom_east_fuya = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_throneroom_east_fuya }
	}
	if = {
		limit = { is_valid_scene_throneroom_east_empire = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_throneroom_east_empire }
	}
	if = {
		limit = { is_valid_scene_garden_east1 = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_garden_east1 }
	}
	if = {
		limit = { is_valid_scene_bedchamber_east1 = yes }
		add_to_variable_list = { name = valid_scenes_list target = flag:talk_scene_bedchamber_east1 }
	}

	# --- Assign to Slots (Up to 10) ---
	# We use a loop-like structure by checking list size and copying
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 1 }
		set_variable = { name = votc_scene_1 value = valid_scenes_list[0] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 2 }
		set_variable = { name = votc_scene_2 value = valid_scenes_list[1] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 3 }
		set_variable = { name = votc_scene_3 value = valid_scenes_list[2] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 4 }
		set_variable = { name = votc_scene_4 value = valid_scenes_list[3] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 5 }
		set_variable = { name = votc_scene_5 value = valid_scenes_list[4] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 6 }
		set_variable = { name = votc_scene_6 value = valid_scenes_list[5] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 7 }
		set_variable = { name = votc_scene_7 value = valid_scenes_list[6] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 8 }
		set_variable = { name = votc_scene_8 value = valid_scenes_list[7] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 9 }
		set_variable = { name = votc_scene_9 value = valid_scenes_list[8] }
	}
	if = {
		limit = { size_of_variable_list = { name = valid_scenes_list } >= 10 }
		set_variable = { name = votc_scene_10 value = valid_scenes_list[9] }
	}
}
