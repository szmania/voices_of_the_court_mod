select_scene = {
	scope = character
	effect = {
		set_global_variable = {
			name = talk_scene
			value = scope:scene
		}
		# Close the picker window
		execute_console_command = "gui.ClearWidgets scene_picker_window"
		
		# Trigger the main conversation event now that the scene is selected
		if = {
			limit = { has_character_flag = is_mcc_convo }
			trigger_event = mcc_event.9001
			remove_character_flag = is_mcc_convo
		}
		else_if = {
			limit = { has_character_flag = is_meet_convo }
			trigger_event = ckms_meet_event.9001
			remove_character_flag = is_meet_convo
		}
		else = {
			trigger_event = talk_event.9001
		}
	}
}

is_valid_scene_throneroom = { is_shown = { is_valid_scene_throneroom = yes } }
is_valid_scene_garden = { is_shown = { is_valid_scene_garden = yes } }
is_valid_scene_tavern = { is_shown = { is_valid_scene_tavern = yes } }
is_valid_scene_market = { is_shown = { is_valid_scene_market = yes } }
is_valid_scene_bedchamber = { is_shown = { is_valid_scene_bedchamber = yes } }
is_valid_scene_dungeon = { is_shown = { is_valid_scene_dungeon = yes } }
is_valid_scene_feast = { is_shown = { is_valid_scene_feast = yes } }
is_valid_scene_temple = { is_shown = { is_valid_scene_temple = yes } }
is_valid_scene_alley = { is_shown = { is_valid_scene_alley = yes } }
is_valid_scene_armycamp = { is_shown = { is_valid_scene_armycamp = yes } }
is_valid_scene_courtyard_night = { is_shown = { is_valid_scene_courtyard_night = yes } }
is_valid_scene_camp = { is_shown = { is_valid_scene_camp = yes } }
is_valid_scene_study = { is_shown = { is_valid_scene_study = yes } }
is_valid_scene_councilchamber = { is_shown = { is_valid_scene_councilchamber = yes } }
is_valid_scene_battlefield = { is_shown = { is_valid_scene_battlefield = yes } }
is_valid_scene_iron_throne = { is_shown = { is_valid_scene_iron_throne = yes } }
is_valid_scene_harem = { is_shown = { is_valid_scene_harem = yes } }
is_valid_scene_lingyinsi = { is_shown = { is_valid_scene_lingyinsi = yes } }

