namespace = invite_to_talk_event

invite_to_talk_event.1002 = {
	immediate = { 
		scope:actor = {
			trigger_event = invite_to_talk_event.1003
		} 
	}
}

invite_to_talk_event.1003 = {
	type = character_event
	theme = faith
	title = talk_event.1001.title
	desc = invite_to_talk_event.1003.desc
	left_portrait = {
		character = scope:recipient
	}
	
	immediate = {
		populate_valid_scenes_list = yes
	}

	option = {
		name = "SELECT_SCENE_OPTION"
		custom_tooltip = "SELECT_SCENE_OPTION_TOOLTIP"
		trigger_event = {
			id = talk_event.9006
		}
	}

	option ={
		name = talk_event.1001.exit
		scope:recipient = {
			add_opinion = {
				target = scope:actor
				modifier = disrespect_opinion
				opinion = -10
			}
		}
	}
}
	
	option ={
		name = talk_scene_bedchamber_string
		trigger = {can_talk_in_bedchamber = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_bedchamber
		}
		
		
		trigger_event = talk_event.9001
	}	
	
	option ={
		name = talk_scene_dungeon_string
		
		trigger = {can_talk_in_dungeon = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_dungeon
		}
		trigger_event = talk_event.9001
	}
	
	option ={
		name = talk_scene_feast_string
		
		trigger = {can_talk_in_feast = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_feast
		}
		trigger_event = talk_event.9001
	}

	option ={
		name = talk_scene_alley_string
		
		trigger = {can_talk_in_alley = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_alley
		}
		
		trigger_event = talk_event.9001	
	}

	option ={
		name = talk_scene_armycamp_string
		
		trigger = {can_talk_in_armycamp = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_armycamp
		}
		
		trigger_event = talk_event.9001	
	}
	option ={
		name = talk_scene_hunt_string
		
		trigger = {can_talk_in_hunt = yes}
		
		set_global_variable = {
			name = talk_scene
			value = flag:talk_scene_hunt
		}
		
		trigger_event = talk_event.9001	
	}


	
	option ={
		name = talk_event.1001.exit
		scope:recipient = {
			add_opinion = {
				target = scope:actor
				modifier = disrespect_opinion
				opinion = -10
			}
		}
	}
}